import numpy as np

print(np.nan)
#it canbe used if you load ahuge dataset or fill null values wit nan
print(np.inf)
#division by zero instead of throwing an exception you can just use this function

print(np.isnan(np.nan))
print(np.isinf(np.inf))

print(np.sqrt(-1))
print(np.array([10])/0)

